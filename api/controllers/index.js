const AuthControllers = require('./auth.controllers')
const UserControllers = require('./user.controller')
const BlogControllers = require('./blogs.controllers')

module.exports = {
    AuthControllers,
    UserControllers,
    BlogControllers
}